# June 2026 Milestone

## Pending

- [x] Fix How It Works connector line alignment — the `h-px` absolute line may break on certain screen widths
- [x] Email confirmation to client on booking — send BR code via email (Nodemailer) so client doesn't lose it if they close the tab
- [x] Email notification to admin on new booking — same Nodemailer setup, two birds one stone
- [x] Admin notes field on `/admin/booking/:id` — `adminNotes` array on `BookingRequest`; `POST /admin/booking/:id/notes` (add), `/notes/:noteId/edit`, `/notes/:noteId/delete`; admin-only, not client-visible
- [x] **Acceptance email to client** — `sendAcceptanceEmail(booking)` fires inside `send-deposit` right after the Stripe invoice is created/finalized/sent (`server.js:836`); code path is complete. Note: this — and the deposit invoice itself — only actually reaches the client in production once a **live Stripe key** is configured; on a test key the emails still fire whenever the route completes without a Stripe error. No longer a code TODO, just an ops/config step.
- [x] **Deposit received notification** — admin gets `sendAdminPaymentAlert(booking, "deposit", amount)` and the client gets an in-app `Notification` (`type: "payment_confirmed"`) from the `invoice.payment_succeeded` webhook handler
- [x] **Final payment received notification** — same webhook handler, `sendAdminPaymentAlert(booking, "final", amount)` + client `Notification` (`type: "payment_confirmed"`)
- [x] Organize uploads by BR Code — BR code generated pre-save in the route handler; multer places files into `uploads/<brCode>/files/<type>/`; a `booking.txt` brief snapshot is written alongside
- [x] Permanent delete cleanup — resolved by design, not a purge job: admin's action was relabeled **Archive** (`POST /admin/booking/:id/archive`, `/admin/bookings/bulk-archive`) and stays a move-to-`_archive/` + `archived: true`, now with a `/admin/booking/:id/restore` and an "Archived" tab on `/admin` so it's not a dead end. Real hard deletion is client-triggered from their own dashboard/project page (`POST /dashboard/booking/:id/delete`) — permanently removes the `files/` folder (active or archived) via `hardDeleteBookingFiles()`, clears `uploadedFiles` and sets `filesDeleted: true` on the `BookingRequest`, but keeps the row and `booking.txt` as a permanent record. No scheduled purge needed since nothing auto-expires.
- [x] Deadline / delivery date field — redefined during scoping into two fields: **deposit due date**, set by admin when sending the deposit invoice (`send-deposit`, editable via `/admin/booking/:id/deposit-due-date`) and enforced by an hourly in-process job (`lib/invoiceExpiry.js`) that auto-declines + voids the Stripe invoice + emails client & admin if it passes unpaid; and **delivery date**, only settable once `depositStatus === "paid"` (`/admin/booking/:id/delivery-date`), shown on `/track`. No work starts, no delivery estimate, without the deposit landing.
- [x] **Admin dashboard pagination** — `/admin` now paginates `BookingRequest.find()` server-side (`skip`/`limit`, 30 per page) instead of loading every booking; the live client-side row-filtering search was replaced with server-side search/status-filter query params (`q`, `field`, `status`) so filtering still works across the full result set, not just the loaded page. Filter pills, field dropdown, and search input now reflect URL state; search debounces (400ms) before navigating.
- [x] **`/hire` form UX improvements** — live character count (`X / 2000`) on the project brief textarea, turning amber near the limit and red at it; `maxlength="2000"` added client- and server-side (`BookingRequest.projectBrief` schema + `/hire` POST validation). File upload area reworked for mobile: drop zone padding/icon shrink under 480px, file list rows wrap onto two lines instead of cramming filename/size/remove button into one, and the remove button got a larger tap target.
- [x] **Bug: client dashboard "Pause project" / "Nudge admin" buttons are dead** — fixed by implementing both routes. `POST /dashboard/booking/:id/pause` sets `status: "paused"` (new enum value on `BookingRequest`) and emails admin via `sendAdminPauseAlert`; blocked once already `declined`/`completed`/`paused`/archived. `POST /dashboard/booking/:id/nudge` just emails admin via `sendAdminNudgeAlert`, no status change (superseded 2026-07-03 — nudge now creates an in-app `AdminNotification` instead of emailing, see below). Added `paused` styling/labels across `dashboard.ejs`, `admin/dashboard.ejs`, `admin/booking.ejs` (status picker), and `track.ejs`; admin resumes a paused project the same way as any other status change (picker on `/admin/booking/:id`). Pause button hides once a project is already paused; bulk-pause skips already-paused rows to avoid duplicate admin emails.
- [x] **Final invoice expiration + stale-payment webhook guard** — `finalDueDate` field added (mirrors `depositDueDate`), set by admin on `send-final` (was hardcoded `days_until_due: 7`), editable via `/admin/booking/:id/final-due-date`, shown on `/track`. `lib/depositExpiry.js` renamed to `lib/invoiceExpiry.js` and gained a final-invoice check: past due + unpaid, it voids the Stripe invoice and resets `finalPaymentStatus`/`finalInvoiceId`/`finalDueDate` (no `status` change — project isn't declined, just unpaid) so admin can resend. Also hardened the `invoice.payment_succeeded` webhook: payments on `archived`/`declined`/`paused` bookings are recorded but no longer auto-flip `status` — admin gets a distinct "payment on inactive project" alert instead, closing the race where a still-live invoice link gets paid after the project's state changed underneath it.
- [x] **Stripe payment button on `/track` (+ client dashboard)** — `BookingRequest` gained `depositInvoiceUrl`/`finalInvoiceUrl`, populated from `finalized.hosted_invoice_url` alongside the existing `depositInvoiceId`/`finalInvoiceId` in `send-deposit`/`send-final`; `finalInvoiceUrl` is cleared on expiry in `lib/invoiceExpiry.js` alongside the other final-invoice fields. `/track`'s query `.select()` now pulls both URL fields plus `finalPaymentStatus`/`finalDueDate` (these two were already read by the view's final-payment banner but had never actually been selected, so that banner was silently dead — fixed as part of this change). "Pay deposit now" / "Pay final invoice now" buttons render in the existing due-date banners, linking straight to the Stripe hosted invoice. Same buttons added to `dashboard-booking.ejs` (client's own project page) and a "Pay now" row-action icon on `dashboard.ejs` (client's project list), since both had the same "invoice sent, check your email" dead-end.
- [x] **Final deliverable download on `/track` (+ client dashboard)** — `BookingRequest` gained `deliverableFiles` (same shape as `uploadedFiles`, plus `uploadedAt`), populated by a new admin-only upload form on `/admin/booking/:id` (`POST /admin/booking/:id/deliverables`, separate `multer` disk storage writing to `uploads/<crCode>/files/deliverables/` so finished output never mixes with client-submitted raw files) with a per-file remove action (`/deliverables/:fileId/delete`). Client download is gated on `status === "completed"`: a public `GET /track/:crCode/deliverables/:filename` route (same trust model as the rest of `/track` — the BR code is the bearer token) and an account-scoped `GET /dashboard/deliverables/:filename` both 403 outside that status. Surfaced on `/track`, `dashboard-booking.ejs` (both gated the same way), and a "Download" row-action icon on `dashboard.ejs`'s project list. A new `deliverable_ready` `Notification` type fires when files are uploaded to an already-completed project, when a project is manually flipped to `completed` with files already attached, and is folded into the existing final-payment-confirmed message when the webhook completes a project that already has files waiting.

## Future Tasks

### Admin QoL
- [ ] Bulk status update on dashboard — update multiple bookings at once as volume grows
- [ ] Date range filter on dashboard — filter bookings by submission date

### Reliability
- [ ] Rate limiting on `/hire` — prevent spam submissions
- [ ] Server-side file type validation on upload — currently only file size is checked; restrict to allowed MIME types
- [ ] Graceful BR code collision handling — surface a clean error if the pre-save loop somehow fails instead of crashing

### Growth
- [ ] Analytics page under `/admin` — bookings per month, revenue by tier, most requested service type

## Completed

- [x] Add favicon — 🎬 emoji favicon via SVG data URI
- [x] Add JSON-LD structured data — marked as `ProfessionalService` with name, description, contact, and service types
- [x] Build the `/hire` booking form — name, location, email, project brief, file upload (up to 250 MB), Telegram fallback, BR code on submission
- [x] Build `/track` page — lookup by BR code; alternate method via name + email combo; toggle between both methods
- [x] Build `/admin` dashboard — lists all bookings, status filter pills, live search by any field (BR code, name, email, location, services, package, status) with custom dropdown
- [x] Build `/admin/booking/:id` detail page — full booking info, status picker, contact quick actions, media links
- [x] Admin file viewer — uploaded files served via `/admin/uploads/:filename` (admin-only); images preview inline, videos play in-browser, audio plays in-browser, all files have a download button
- [x] Renamed CR Code → BR Code (Booking Request Code) across all views and copy
- [x] Hero CTA — replaced "Check Out Recent Projects" button with "Track a Project" linking to `/track`
- [x] **Stripe Invoice — deposit (30%)** — `POST /admin/booking/:id/send-deposit`; creates Stripe customer, posts invoice item, creates + finalizes + sends invoice; stores `agreedPrice`, `stripeCustomerId`, `depositInvoiceId` on booking; sets `depositStatus: pending`
- [x] **Stripe Invoice — final (70%)** — `POST /admin/booking/:id/send-final`; reuses existing Stripe customer, posts 70% invoice item, finalizes + sends; stores `finalInvoiceId`; sets `finalPaymentStatus: pending`
- [x] **Stripe webhook** — `POST /webhooks/stripe` registered before `express.json()` with raw body; verifies signature; on `invoice.payment_succeeded` advances `depositStatus`/`finalPaymentStatus` to `paid` and flips booking `status` to `in-progress` / `completed`
- [x] **BookingRequest schema additions** — `agreedPrice`, `stripeCustomerId`, `depositInvoiceId`, `finalInvoiceId`, `depositStatus` (none/pending/paid), `finalPaymentStatus` (none/pending/paid)
- [x] **Admin booking UI — payment card** — full state machine: price input + "Send Deposit Invoice (30%)" (disabled until price entered and status ≥ in-review) → "Deposit invoice sent, awaiting payment" → "Deposit received + Send Final Invoice (70%)" → "Final invoice sent, awaiting payment" → "All payments received"
- [x] **Client account system** — `User` model (bcrypt-hashed password); `/login`, `/logout`, inline `/signup` from `/hire/success` (links the just-submitted booking via crCode); `/hire` and `/login?cr=` both auto-link a booking to the logged-in/just-created account
- [x] **Client dashboard** — `/dashboard` (all bookings + payment progress), `/dashboard/new` (profile-completeness gate before starting a fresh booking), `/dashboard/booking/:id` (detail + revision requests), `/dashboard/gallery` (uploaded files across all projects), `/dashboard/account` (profile edit, password change, account deletion)
- [x] **Client-submitted revisions** — clients can post revision request messages on `/dashboard/booking/:id`; admin marks each reviewed from `/admin/booking/:id`
- [x] **In-app notifications** — `Notification` model; created on status change, invoice sent, payment confirmed, and project dismissal (account-linked bookings only); `/dashboard/notifications` page + `/api/notifications/poll` for a live unread badge
- [x] **Coupon system** — `Coupon` model (percent/fixed, optional expiry, active toggle); `/admin/coupons` CRUD UI; `/hire/coupon/validate` AJAX check + server-side re-validation on submit; discount applied to booking subtotal and reflected in `booking.txt`
- [x] **Booking archive (soft delete)** — `POST /admin/booking/:id/archive` and `/admin/bookings/bulk-archive` set `archived: true` instead of removing the record, move the booking's `uploads/<crCode>/` folder to `uploads/_archive/`, and notify the linked client; `POST /admin/booking/:id/restore` reverses it; `/admin?view=archived` tab keeps archived bookings reachable; file-serving routes check both the active and archived path
- [x] **Direct file upload replaces Telegram-only delivery** — `/hire` now accepts up to 20 files (250MB each) via `multer`, stored under `uploads/<brCode>/files/<type>/`; Telegram handle kept only as an optional fallback for oversized files
- [x] **In-app admin notifications for client nudges** — `AdminNotification` model replaces the old `sendAdminNudgeAlert` email; `POST /dashboard/booking/:id/nudge` now creates a notification record and is rate-limited to 3 per booking per rolling hour (`429` past that); `/admin/notifications` page + `/api/admin/notifications/poll` bell badge (15s polling, toasts new nudges) wired into every admin view via `_notif-poll.ejs`
- [x] **Session store moved to MongoDB** — `express-session` now backed by `connect-mongo` (`MongoStore`) instead of the default in-memory store, so admin/client sessions survive server restarts
- [x] **Client booking detail page overhaul** — `dashboard-booking.ejs` reworked into a two-column layout (main content + sticky payment/status sidebar) with submitted files grouped and collapsible by media type (Video/Audio/Image/Other)
- [x] **Client hard-delete also archives** — `POST /dashboard/booking/:id/delete` now sets `archived: true` and moves the emptied folder into `uploads/_archive/` in addition to deleting files, so a client-deleted project drops out of the active admin view too
